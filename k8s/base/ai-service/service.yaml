apiVersion: v1
kind: Service
metadata:
  name: ai-service
spec:
  selector:
    app: ai-service
  ports:
    - port: 8001
      targetPort: 8001
  clusterIP: None  # Internal only — never exposed outside cluster
